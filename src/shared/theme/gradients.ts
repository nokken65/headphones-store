import { colors } from './colors'

const redGradient = `linear-gradient(75deg, ${colors.red[300]} -7.33%, ${colors.red[500]} 90.21%)`
const blueGradient = `linear-gradient(75deg, ${colors.blue[300]} -7.33%, ${colors.blue[500]} 90.21%)`
const cyanGradient = `linear-gradient(75deg, ${colors.cyan[500]} -7.33%, ${colors.cyan[700]} 90.21%)`

export { blueGradient, cyanGradient, redGradient }
